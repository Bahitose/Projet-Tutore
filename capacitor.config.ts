import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_paiement',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
